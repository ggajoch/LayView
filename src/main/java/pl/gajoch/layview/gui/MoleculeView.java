package pl.gajoch.layview.gui;

public class MoleculeView extends MovableSubScene {
    public MoleculeView (GraphicsWindowManager parent, double width, double height) {
        super(parent, width, height);
        /*Builder builder = new Builder();

        scene.setRoot(builder.getRoot());
        builder.handlers_set(scene);
        builder.cam_set(scene);*/
    }
}
